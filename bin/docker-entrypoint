#!/bin/bash -e

# Prepare the database on every boot (idempotent: creates DB if missing,
# runs any pending migrations). Works regardless of how the server is invoked.
./bin/rails db:prepare

exec "${@}"
